# Python tooling for developing and testing this project.
# Install into a virtualenv:  make venv   (or: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt)
ansible-core==2.21.3
ansible-lint==26.8.0
molecule==26.8.0
molecule-plugins[docker]==26.7.15
yamllint==1.38.0
pre-commit==4.6.2
pytest==9.1.1
ruff==0.16.6
# Required on the controller by the community.proxmox collection
proxmoxer==2.3.0
requests==2.32.5
# Required by the Docker driver for Molecule
docker==7.1.0
