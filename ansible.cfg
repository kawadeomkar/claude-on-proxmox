[defaults]
# A directory: controller.yml plus your own hosts.yml. VMs are not listed;
# playbooks/discover.yml adds them at runtime.
inventory = inventory
# Extends Ansible's own defaults rather than replacing them - setting this key
# discards them, and a stray hosts.yml.bak would then be parsed as inventory.
inventory_ignore_extensions = .pyc, .pyo, .swp, .bak, ~, .rpm, .md, .txt, .rst, .orig, .cfg, .retry, .example
roles_path = roles
filter_plugins = filter_plugins
interpreter_python = auto_silent
retry_files_enabled = false
callback_result_format = yaml
callbacks_enabled = ansible.posix.profile_tasks
# Shared cache so re-runs of `site.yml` do not re-gather facts for every play.
gathering = smart
fact_caching = jsonfile
fact_caching_connection = .cache/facts
fact_caching_timeout = 3600
forks = 10

[ssh_connection]
pipelining = true
# accept-new: trust a freshly provisioned VM's host key on first contact but
# still refuse if a known host's key changes later.
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=accept-new
